// const express = require('express');
// const { auth } = require('../middlewares/Auth');
// const { createSubject } = require('../controllers/Subject'); // Correct the import for createSubject
// const { createTimetable } = require('../controllers/Timetable'); // Correct the import for createTimetable

// const router = express.Router(); // Initialize the router

// // Route for adding timetable (should call createTimetable controller)
// router.post('/add-schedule', auth, createTimetable);

// // Route for adding subject (should call createSubject controller)
// router.post('/add-subject', auth, createSubject);

// module.exports = router;
